// import GeoTIFF from 'geotiff';
const GeoTIFF = require('geotiff');

// console.log(process.env.NODE_OPTIONS);

// TODO: debugging with npm doesn't work using NODE_OPTIONS

const VSOP = require('astronomia').data;
var foo = 'bar';
foo = 'baz';

// console.log(VSOP);
