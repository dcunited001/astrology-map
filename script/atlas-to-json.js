import args from 'args';

// const AtlasParser = require('./atlas-parser');

args.option('input')

console.log(AtlasParser);

// function atlasToJSON(in, out) {
//   const outfile = File. JSON.stringify(AtlasParser.parse(in).
// }

// atlasToJSON(in, out);